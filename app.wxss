page {
  min-height: 100%;
  color: #17201e;
  background: #f5f7f8;
  font-size: 28rpx;
}

button::after {
  border: 0;
}

.safe-bottom {
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
}
