page {
  min-height: 100%;
  color: #17201e;
  background: #f6f7f3;
  font-size: 28rpx;
}

button::after {
  border: 0;
}

.safe-bottom {
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
}
